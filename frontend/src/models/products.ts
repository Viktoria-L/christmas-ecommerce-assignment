export interface Products {
    id: number;
    name: string;
    price: number;
    description: string;
    image: string,
    stock: number,
}

//Tänkte bygga upp interface för produkterna och lägga in